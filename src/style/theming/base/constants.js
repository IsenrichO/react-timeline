import { THEME_RED } from './colors';
import { DEFAULT_FONTS, HELVETICA_NEUE } from './fonts';
import { BUTTON_TIMING_FUNCTION } from './transitions';

/* TRUE CONSTANTS */
export const APP_BAR_HEIGHT = 64;

/* COMPILED DEFAULT EXPORT */
export default {
  APP_BAR_HEIGHT,
  BUTTON_TIMING_FUNCTION,
  DEFAULT_FONTS,
  HEIGHT_OFFSET_VP: `calc(100vh - ${APP_BAR_HEIGHT}px)`,
  HELVETICA_NEUE,
  THEME_RED,
};
