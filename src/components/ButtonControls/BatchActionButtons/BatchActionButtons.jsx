import BatchActionButtonsPure from './BatchActionButtons.Pure';
import styler from '../../../style/styler';

export default styler(({ colors, fonts }) => ({
  batchActionBtns: {},
}), {
  styleName: 'BatchActionButtonStyles',
})(BatchActionButtonsPure);
