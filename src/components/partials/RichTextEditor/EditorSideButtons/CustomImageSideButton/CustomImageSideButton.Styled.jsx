import CustomImageSideButtonPure from './CustomImageSideButton.Pure';
import styler from '../../../../../style/styler';

export default styler(({ colors, fonts, helpers, keywords }) => ({
  // Static `className` declarations necessary for nested references:

}), {
  styleName: 'CustomImageSideButtonStyles',
})(CustomImageSideButtonPure);
