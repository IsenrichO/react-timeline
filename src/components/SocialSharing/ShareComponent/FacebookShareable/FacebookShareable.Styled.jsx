import FacebookShareablePure from './FacebookShareable.Pure';
import styler from '~/style/styler';

export default styler(({ colors, fonts, helpers, keywords }) => ({

}), {
  styleName: 'FacebookShareableStyles',
})(FacebookShareablePure);
